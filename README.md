# CP-ZonTana

## Setup Instructions

### Prerequisites

- Node.js and npm installed

### Installation

1. **Clone the repository**

```bash
   git clone https://github.com/xerlaxtole/CP-ZonTana
```

2. **Navigate to frontend directory**

```bash
   cd frontend
```

3. **Install dependencies**

```bash
   npm i
```

4. **Configure environment variables**
   - Create a `.env` file in the root directory
   - Use `.env.example` as a template
   - **Note:** Environment secrets are available in Discord (Pinned in Network-Project channel)

5. **Start the application**

   **Local development:**

```bash
   npm start
```

**Production mode:**

```bash
   npm run prod
```

The application should now be running at http://localhost:3000/

## Deployment

The server is deployed on Railway didn't need to configure anything in server/ .
